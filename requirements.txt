# Trilytics MAI pipeline dependencies.
#   pip3 install -r requirements.txt
# Python >= 3.9. Everything else is stdlib, plus urllib3/google-api-core which
# ship as dependencies of the two packages below.

requests>=2.28          # all HTTP fetching (CKAN, data.gov.in, Ni-kshay, PMJAY, DHS, geoBoundaries)
firebase-admin>=6.0     # Firestore publishing (pulls in google-cloud-firestore + google-auth)

# analysis notebook (notebooks/MAI_analysis.ipynb)
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
nbformat>=5.9
nbclient>=0.9
ipykernel>=6.25
